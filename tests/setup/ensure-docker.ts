// DEPRECATED: Docker enforcement removed — tests run on host/devcontainer.
// Previously threw if not inside Docker container.
// Kept as no-op for backward compatibility with vitest.config.ts setupFiles.
