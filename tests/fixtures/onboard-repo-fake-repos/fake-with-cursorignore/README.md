# fake-with-cursorignore

Python API fixture + `.cursorignore` with sensitive patterns + real `secrets/key.json`. Used by FR-17 / EC-3 / AC-17 / ONBOARD028 — verify Phase 0 subagents skip ignored paths.
