[project]
name = "fake-no-tests"
version = "0.1.0"
description = "Python API fixture WITHOUT tests/ — UC-7 baseline test skip"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
