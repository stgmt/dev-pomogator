# Functional Requirements — minimal-spec

### FR-1: Single requirement with nothing covering it

The spec has exactly one FR and nothing else — no AC, no scenarios, no tasks,
no file changes. The builder must still produce a valid (if sparse) SpecGraph
without crashing.
