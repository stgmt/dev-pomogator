# Design

## Architecture

Browser or command-line client calls same-origin routes. The route calls `get_trace` for all card inventory, complete directed graph relationships, evidence and history.

## BDD Test Infrastructure

Classification: TEST_DATA_ACTIVE

HTTP route tests prove the experience. Browser automation, bundle startup and dependency-absent runtime are deferred.
