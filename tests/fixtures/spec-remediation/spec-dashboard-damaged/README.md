# Damaged spec-dashboard dogfood fixture

This is a deliberately incomplete, producer-shaped reconstruction of the pre-repair dashboard specification from the 2026-08-13/14 dogfood session. Tests copy it to `V4World.tempDir`; no test writes canonical `.specs/spec-dashboard/`.

Known defects are enumerated in `../expected-findings.json`. The fixture is intentionally NOT an implementation-ready product specification.

<!-- Deterministic safe-repair marker used by SPECGEN004_715_MIRROR. -->
remediation-safe-marker: old
