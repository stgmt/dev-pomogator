# spec-MCP dogfood dataset — runtime evidence for the v4 scope update

Generated by `tools/spec-mcp-server/dogfood-dataset.ts` — drives every tool's REAL handler
against the REAL `.specs/` graph (47 specs). This is the evidence a scope update should be
driven from, NOT grep guesses. Sampled inputs: `node_id=FR-2`, `tag=@plugin`,
`scenario=SCEN-core024-01…`, `anchor=AC-1`, `phase=∅`.

## Per-tool result

| Tool | consumer? (grep) | returns data? (runtime) | size | verdict |
|------|:---:|:---:|---:|---|
| `find_refs` | none | **YES** | **86 refs** | live engine, no consumer → keep + surface (spec-graph-query). NOT a Marksman dup. |
| `conformance_check` | yes | YES | 1250 findings | core, keep |
| `search` | yes | YES | 50 results | core, keep |
| `get_coverage_summary` | none | YES | 48 specs | works; near-dup of get_coverage by name only (counts vs honest DONE) → keep, doc the difference |
| `list_specs` | none | YES | 47 specs | works, no consumer → surfaced via spec-graph-query |
| `find_by_tags` | none | YES | 16 scenarios | works, no consumer → surfaced |
| `find_orphans` | yes | YES | 11 findings | core, keep |
| `get_test_result` | yes | YES | object | core, keep |
| `get_node` | none | YES | 1 node | works, no consumer → surfaced |
| `validate_anchor` | none | YES (registered) | 1 | works; narrow (bulk+fix is anchor-fix) → surfaced |
| `get_coverage` | yes | YES | object | core (FR-32 honesty gate), keep |
| `get_trace` | yes | **NO** | 0 scenarios | ⚠️ PRIMARY tool returned ZERO scenarios for FR-2 — investigate (trace edge gap or FR-2 genuinely untraced) |
| `list_phase_tasks` | none | **NO** | 0 tasks | ⚠️ Task nodes carry no `phase` (sampled phase was ∅) — the tool can't work until the TASKS parser populates `phase`. Under-functional, not just unconsumed. |

## What this changes for the scope update

1. **"Dead by no-consumer" ≠ "broken".** 5 of the 7 grep-dead tools return rich real data
   (`find_refs` 86, `get_coverage_summary` 48, `list_specs` 47, `find_by_tags` 16, `get_node` 1).
   They are working engines with no caller — the right move was making them DISCOVERABLE
   (`spec-graph-query` skill), not deleting them. The dataset confirms it empirically.
2. **`find_refs` is the strongest case** — 86 semantic references on real data. Keep, and the
   earlier "redundant with Marksman" label is decisively wrong (Marksman has no such edges).
3. **Two genuine runtime BUGS** (confirmed by probing the whole corpus, not one sample):
   - **`get_trace` returns `scenarios=0` for ALL 47 FRs** (probed FR-1..FR-6 + the sample: every
     one has AC + tasks but ZERO scenarios). The FR→Scenario trace edge is missing — the PRIMARY
     traceability tool cannot answer "what tests this requirement". (`find_refs` finds 86 refs
     for the same node because it traverses differently.) Real bug, high value — get_trace is the
     headline tool used by skills.
   - **`list_phase_tasks` → 0 because 0 of 110 Task nodes carry a `phase` field.** The TASKS
     parser never populates `phase`, so the tool's only input matches nothing → non-functional
     by construction. Fix = populate `phase` in the parser (assign each task its enclosing
     `## Phase …` heading), or drop the tool.

## Reproduce
```
node --import tsx tools/spec-mcp-server/dogfood-dataset.ts > dataset.json
```
