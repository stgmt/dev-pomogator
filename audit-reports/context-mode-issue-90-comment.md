Cross-linking canonical context-mode issue: #139 now contains the context-mode integration/runbook material and references this `ENABLE_TOOL_SEARCH=1` task as related MCP-startup diet work.

I am not closing this issue because provisioning `ENABLE_TOOL_SEARCH=1` is still a separate default-settings implementation task, not the same fix as context-mode MCP health/reconnect.
