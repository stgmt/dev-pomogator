Step-by-step reasoning for constructing the test command with --framework jest:

1. UNDERSTANDING THE REQUEST
   The user invoked "/run-tests --framework jest", explicitly requesting the jest test framework.

2. PROJECT ANALYSIS
   - package.json shows this project uses vitest (devDependency), NOT jest.
   - There is no jest.config.ts/js/cjs in the project.
   - The project's own test command is: npm run test:e2e -> bash scripts/docker-test.sh
   - Tests live in tests/e2e/*.test.ts and use vitest.
   - The docker-compose.test.yml runs tests inside a Docker container.

3. TEST RUNNER WRAPPER ARCHITECTURE
   The project has a test runner wrapper system:
   - extensions/tui-test-runner/tools/tui-test-runner/test_runner_wrapper.ts is the canonical v2 wrapper
   - extensions/test-statusline/tools/test-statusline/test_runner_wrapper.sh is a thin shim that delegates to the .ts wrapper
   - The wrapper accepts --framework <name> and a test command after --
   - It supports: vitest, jest, pytest, dotnet, rust, go
   - It parses test output via framework-specific adapters (JestAdapter for jest)
   - It writes YAML status files for the TUI/statusline to consume

4. WRAPPER INVOCATION PATTERN
   From test_runner_wrapper.sh, the wrapper is invoked as:
     npx tsx <path-to-wrapper.ts> [--framework <name>] -- <test-command> [args...]

   The wrapper:
   - Accepts --framework jest to select the JestAdapter
   - Requires a test command after -- (e.g., "npx jest")
   - Spawns the test command as a child process
   - Parses stdout/stderr through the adapter for status updates

5. FRAMEWORK SELECTION
   Since the user explicitly passed --framework jest:
   - The wrapper's parseArgs() will set framework = 'jest'
   - resolveFramework() will use 'jest' directly (skipping auto-detection)
   - The JestAdapter will be used to parse output

6. JEST COMMAND
   Since there is no jest configuration in this project, the actual jest command
   would simply be "npx jest" (using npx since jest is not a direct dependency).
   In a real project with jest configured, this would be the standard invocation.

7. FINAL COMMAND CONSTRUCTION
   The wrapper is the TypeScript file at:
     extensions/tui-test-runner/tools/tui-test-runner/test_runner_wrapper.ts

   The full command:
     npx tsx extensions/tui-test-runner/tools/tui-test-runner/test_runner_wrapper.ts --framework jest -- npx jest

   This passes --framework jest to the wrapper (selecting JestAdapter for output parsing),
   and "npx jest" as the actual test command to execute.

8. NOTES
   - The .dev-pomogator/tools/ path could also be used if the extension is installed there,
     but extensions/ is the source of truth per CLAUDE.md.
   - If TEST_STATUSLINE_SESSION env var is not set, the wrapper falls through to
     passthrough mode (runs the command directly without YAML status tracking).
   - The project itself does not use jest -- it uses vitest. Running jest here would
     likely fail unless jest is installed. But the user explicitly requested --framework jest,
     so we honor that request.
