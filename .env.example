# ============================================
# dev-pomogator — environment variables
# Generated by installer. Copy to .env and fill values.
# ============================================

# --- LLM key (shared) ---
# [REQUIRED] API key for the aipomogator.ru LLM endpoint. Powers THREE hooks, all of which
# read it from THIS .env (cwd) at runtime: auto-commit messages, prompt-suggest next-step
# hints (the "pinator"), and the claim-evidence judge. Copy this file to .env and fill it.
AUTO_COMMIT_API_KEY=sk-xxx

# [optional] Custom LLM endpoint (default: aipomogator.ru)
# AUTO_COMMIT_LLM_URL=https://aipomogator.ru/go/v1

# [optional] LLM model name
# AUTO_COMMIT_LLM_MODEL=openrouter/deepseek/deepseek-v3.2

# --- prompt-suggest ---
# [optional] OpenRouter API key for Haiku LLM (priority over AUTO_COMMIT_API_KEY)
# OPENROUTER_API_KEY=sk-or-v1-xxx

# [optional] Fallback API key (aipomogator.ru) if OPENROUTER not set
# AUTO_COMMIT_API_KEY=sk-xxx

# [optional] Enable/disable prompt suggestions (default: true)
# PROMPT_SUGGEST_ENABLED=true

# [optional] Suggestion TTL in ms (default: 600000 = 10 min)
# PROMPT_SUGGEST_TTL=600000

# --- native-statusline ---
# [optional] Set to "off" to stop dev-pomogator from auto-installing the native
# Claude Code statusLine (ccstatusline) into ~/.claude/settings.json.
# Default (unset) = on. A user's own custom statusLine is never overwritten.
# DEV_POMOGATOR_STATUSLINE=off

# --- test-statusline ---
# [optional] Enable/disable test statusline extension
# TEST_STATUSLINE_ENABLED=true

# --- tui-test-runner ---
# [optional] Enable/disable the canonical status v2 pipeline
# TEST_STATUSLINE_ENABLED=true

# [optional] Framework override for canonical status v2 writer: vitest|jest|pytest|dotnet|rust|go|auto
# TEST_STATUSLINE_FRAMEWORK=auto
