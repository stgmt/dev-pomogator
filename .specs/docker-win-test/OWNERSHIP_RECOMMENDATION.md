# Ownership Recommendation — docker-win-test

> Generated by `owner-picker` resolver from cross-spec-reconcile.

## Contested Path

`tests/hyperv-scenarios/`

## Analysis

- **First commit date:** 2026-04-13
- **docker-win-test created:** 2026-05-22
- **hyperv-test-runner created:** 2026-05-22
- **Canonical owner (by proximity to first commit):** `docker-win-test`

## Recommendation

The path `tests/hyperv-scenarios/` should be owned by spec `docker-win-test`.
Spec `hyperv-test-runner` claims the same path but was created further from the file's
first-commit date in git history. Consider updating `hyperv-test-runner` to reference the
asset via the canonical owner's module instead.

## Next Steps

1. Review the ownership claim in `hyperv-test-runner` spec files (DESIGN.md, TASKS.md).
2. Update cross-references to point to `docker-win-test` module.
3. Remove duplicate module claims from `hyperv-test-runner`.
4. Re-run `cross-spec-reconcile` to confirm conflict is resolved.
