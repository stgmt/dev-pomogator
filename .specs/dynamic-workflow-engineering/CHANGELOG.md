# Changelog

All notable changes to this feature specification will be documented in this file.

## [Unreleased]

### Added
- Complete FR, AC, CHK, Decision, Story, and BDD trace for Workflow-only native Agent delegation.
- Provided `dynamic-workflow-engineering` skill preserved as a normative spec artifact.
- Two dogfood incident contracts for unbounded retries, barriers, partial useful output, and journal-first classification.
- Distribution parity, sanitized audit, circuit breaker, partial-result, and guarantee-tier requirements.
- External workflow pattern research queue with verification markers.

### Changed
- Replaced the initial nested-plugin assumption with one existing marketplace plugin plus bundled skill and optional executable runtime.
- Replaced metric-only runaway classification with logical-call, physical-attempt, output, failure, coverage, and reproduction evidence.

### Fixed
- Removed scaffold anchor placeholders through a graph-consistent document transaction.
- Eliminated direct-Agent compatibility exceptions from the target architecture; existing consumers must migrate behind Workflow contracts.

## [0.1.0] - TBD

### Added
- Planned initial implementation.

## [Unreleased] — second dogfood harness contract

### Added
- Root/worktree/base-SHA/owner/dirty-path admission and atomic Agent→root→process-group→lease→run→proof binding requirements.
- CAS single-writer state, separate checkout/runtime leases, process-group stop evidence, resumability states, HARNESS_REPAIR cutoff, and bounded recovery capsule requirements.
- Typed captured-process, canonical-probe, typed-summary, transactional-mutation, per-run journal, monitor/watchdog, resource-label, and mount-validation contracts.
- Provenance-only second-incident fixture plan and explicit `REPLAY_UNAVAILABLE` boundary.

### Changed
- Expanded all 13 existing FR/AC/source-scenario/task traces without adding duplicate FRs or new Dynamic Workflow ownership to `spec-generator-v4`.
- Kept adjacent-project commits, model names, container names, reported tests, and metrics as unverified context rather than dev-pomogator evidence.

### Not claimed
- No production code, tests, task completion, authoritative replay, or implementation-ready guarantee tier is claimed by this specification-only update.
