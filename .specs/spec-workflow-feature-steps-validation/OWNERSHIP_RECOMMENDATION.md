# Ownership Recommendation — spec-workflow-feature-steps-validation

> Generated by `owner-picker` resolver from cross-spec-reconcile.

## Contested Path

~~`src/installer/memory.ts`~~ (removed in v2 — no canonical replacement)

## Analysis

- **First commit date:** 2026-01-18
- **spec-workflow-feature-steps-validation created:** 2026-05-22
- **spec-workflow-md-validation created:** 2026-05-22
- **Canonical owner (by proximity to first commit):** `spec-workflow-feature-steps-validation`

## Recommendation

The path ~~`src/installer/memory.ts`~~ (removed in v2 — no canonical replacement) should be owned by spec `spec-workflow-feature-steps-validation`.
Spec `spec-workflow-md-validation` claims the same path but was created further from the file's
first-commit date in git history. Consider updating `spec-workflow-md-validation` to reference the
asset via the canonical owner's module instead.

## Next Steps

1. Review the ownership claim in `spec-workflow-md-validation` spec files (DESIGN.md, TASKS.md).
2. Update cross-references to point to `spec-workflow-feature-steps-validation` module.
3. Remove duplicate module claims from `spec-workflow-md-validation`.
4. Re-run `cross-spec-reconcile` to confirm conflict is resolved.
