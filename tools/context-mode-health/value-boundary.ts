export function renderContextModeValueBoundary(): string {
  return [
    '# context-mode value boundary',
    '',
    'context-mode is valuable when the agent must derive answers over large raw artifacts, retain session knowledge across compaction, or search indexed evidence without flooding the live context window.',
    '',
    'Disciplined `rg`, shell pipes, and small targeted reads can be parity for ordinary source-code inspection. Context trimming also does not automatically translate into realized subscription-dollar savings for every workflow.',
    '',
    'Do not claim universal daily usage reduction unless a measured workflow-specific baseline supports the number.',
  ].join('\n');
}
