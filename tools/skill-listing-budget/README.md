# skill-listing-budget — tool

Self-contained script that writes `skillListingBudgetFraction: 1.0` to
`~/.claude/settings.json` on every Claude Code SessionStart.

Idempotent. Atomic. Fail-open.

Spec — [`.specs/skill-listing-budget/`](../../../../.specs/skill-listing-budget/).
