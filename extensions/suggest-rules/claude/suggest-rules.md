---
description: "Анализ сессии и предложение Claude rules на основе выявленных паттернов"
allowed-tools: Read, Write, Glob, Grep
argument-hint: "[mem|nomem|session|global|project|verbose]"
---

# Suggest Claude Rules

## Mission

Проведи ретроспективный анализ текущей сессии и предложи Claude rules для сохранения ценного контекста и паттернов.

---

## Phase -1: Memory Context (Claude-mem) — ОПЦИОНАЛЬНО

> ⚠️ **Эта фаза опциональна.** Если claude-mem недоступен — тихо пропусти и начни с Phase 0.

### Pre-Check: Определение доступности

**Проверь доступность через ListMcpResourcesTool:**
```javascript
ListMcpResourcesTool({ server: "plugin_claude-mem_mcp-search" })
// Если ошибка или пустой результат → claude-mem недоступен
```

**Результат проверки:**

| Статус | Действие |
|--------|----------|
| ✅ MCP доступен | Продолжить Phase -1 |
| ❌ MCP недоступен | → Тихо перейти к Phase 0 (БЕЗ сообщений об ошибке) |
| ⚠️ Нет результатов | → Тихо перейти к Phase 0 |

---

**Если доступен MCP сервер claude-mem, найди кандидатов для rules из истории проекта.**

> ⚡ Claude-mem результаты НЕ проходят Quality Filter — они уже отфильтрованы при сохранении.

### 1. Поиск паттернов и решений

```javascript
// Поиск записей с паттернами и архитектурой
mcp__plugin_claude-mem_mcp-search__search({
  query: "pattern architecture workflow principle",
  limit: 30
})

// Результат — индекс:
// | ID | Time | T | Title | Read |
// | #172 | 4:45 PM | 🔵 | OpenAI-Compatible API Router... | ~495 |
// | #163 | 8:57 AM | 🔵 | Claude-mem Complete Workflow... | ~887 |
```

**Типы записей:**
- 🔵 discovery — найденные паттерны, архитектура
- 🔴 bugfix — исправления (могут содержать anti-patterns)
- ⚖️ decision — принятые решения
- 🟣 feature — новая функциональность

### 2. Получение деталей релевантных записей

```javascript
// Запросить полные данные по ID
mcp__plugin_claude-mem_mcp-search__get_observations({
  ids: [172, 163, 162]  // числа, не строки!
})

// Каждая запись содержит:
{
  "id": 172,
  "type": "discovery",
  "title": "OpenAI-Compatible API Router...",
  "subtitle": "Relay router implements...",
  "facts": ["Router implements POST /responses...", "..."],
  "narrative": "Examined the relay router... The consistent pattern of POST endpoint → relay format type → controller.Relay() suggests a plugin-style architecture...",
  "concepts": ["how-it-works", "pattern"],
  "files_read": ["source-code/.../relay-router.go"]
}
```

### 3. Извлечение кандидатов для rules

Из каждой записи с `"pattern"` в concepts извлечь:

| Поле | Откуда брать | Пример |
|------|--------------|--------|
| **Название** | title → kebab-case | `relay-router-plugin-pattern` |
| **Суть** | subtitle или первое предложение narrative | "Plugin-style architecture для добавления новых endpoint types" |
| **Факты** | facts[] — конкретные примеры | "POST endpoint → relay format → controller.Relay()" |
| **Источник** | id | #172 |

### 4. Формирование списка

**Сохрани кандидатов из claude-mem для Phase 3 (Index Output).**

Они будут показаны в секции `🧠 From Memory` БЕЗ прохождения Quality Filter.

**Если claude-mem недоступен или нет результатов:**
- НЕ выводить сообщения об ошибке
- НЕ упоминать отсутствие claude-mem пользователю
- Тихо начать с Phase 0
- В Phase 3 показать только секции 🌍 Global и 📁 Project (без 🧠 From Memory)

---

## Phase 0: Изучение структуры rules

**Сначала изучи существующие rules в проекте:**

```
Glob(".claude/rules/**/*.md") → список файлов
```

**Извлеки:**
- Категории (папки): core/, development/, testing/...
- Количество rules в каждой
- Примеры naming convention

**Выведи:**
```
## Структура rules в проекте

Найдено N rules в K категориях:
- category1/ (X файлов)
- category2/ (Y файлов)
...

(или: Папка .claude/rules/ не найдена — будет создана)
```

---

## Phase 1: Session Analysis

Проанализируй **каждый turn** разговора от начала до этой команды:

**Что искать:**
- 🟢 **Успехи**: Какие паттерны привели к правильному решению?
- 🔴 **Ошибки и исправления**: Где подход провалился? Что исправил пользователь?
- 📁 **Файлы**: Какие файлы обсуждались? Какие паттерны в них?
- 🔧 **Решения**: Какие архитектурные решения были приняты?
- 📚 **Domain knowledge**: Какие специфические знания о проекте раскрыты?

---

## Phase 2: Quality Filter (AI Pre-Check)

Для каждого потенциального правила выполни проверку:

| Check | Критерий | Вопрос |
|-------|----------|--------|
| A1 | **Universal** | Применимо к разным проектам/задачам? |
| A2 | **Abstracted** | Общий принцип, не привязан к конкретному багу? |
| A3 | **High-Impact** | Предотвращает реальные проблемы? |

**Формат проверки:**

```
Кандидат: "fail-fast-principle"
├── A1 Universal:    ✅ применимо к любому backend
├── A2 Abstracted:   ✅ общий принцип
├── A3 High-Impact:  ✅ предотвращает silent failures
└── RESULT: ✅ PASS

Кандидат: "fix-specific-jwt-bug"
├── A1 Universal:    ❌ специфично для этой задачи
├── A2 Abstracted:   ❌ привязано к конкретному багу
├── A3 High-Impact:  ⚠️ средний
└── RESULT: ❌ REJECT
```

**Выводи ТОЛЬКО правила с PASS.**

---

## Phase 3: Index Output

Выведи детальный индекс с обоснованием. **Три источника кандидатов:**

```
## Найдено 4 правила

### 🧠 From Memory (2) — из claude-mem

> Эти кандидаты извлечены из истории проекта (concepts: "pattern")

**1. relay-router-plugin-pattern**
- 📝 Суть: Plugin-style architecture для endpoint routing
- 💡 Факт: "POST endpoint → relay format type → controller.Relay()"
- 🔗 Источник: #172 (🔵 discovery)
- 📍 Рекомендация: 📁 Project → infrastructure/

**2. queue-based-async-processing**
- 📝 Суть: Разделение быстрого capture и медленного processing
- 💡 Факт: "Hooks execute <100ms by enqueuing, worker does AI compression 5-30s"
- 🔗 Источник: #163 (🔵 discovery)
- 📍 Рекомендация: 🌍 Global

### 🌍 Global (1) — из анализа сессии

**3. fail-fast-logging**
- 📝 Суть: Логирование перед re-throw исключений
- 🎯 Зачем: Отладка без догадок
- 📍 Откуда: Исправление бага в payment webhook

### 📁 Project (1) — из анализа сессии

**4. channel-pricing-validation**
- 📝 Суть: Проверка Input=0 для image моделей
- 🎯 Зачем: Предотвращение model_price_error
- 📍 Рекомендация: billing/ (рядом с pricing-domain.md)

---
Выбор: `1,2,4` | `all` | `mem` (только 1-2) | `session` (только 3-4) | `n` отмена
```

**Если claude-mem был недоступен или не дал результатов:**

```
## Найдено 2 правила

### 🌍 Global (1) — из анализа сессии

**1. fail-fast-logging**
- 📝 Суть: Логирование перед re-throw исключений
- 🎯 Зачем: Отладка без догадок
- 📍 Откуда: Исправление бага в payment webhook

### 📁 Project (1) — из анализа сессии

**2. channel-pricing-validation**
- 📝 Суть: Проверка Input=0 для image моделей
- 🎯 Зачем: Предотвращение model_price_error
- 📍 Рекомендация: billing/ (рядом с pricing-domain.md)

---
Выбор: `1,2` | `all` | `n` отмена
```

> Секция 🧠 From Memory отсутствует — это нормально, claude-mem опционален.

**Ожидай ответ пользователя перед Phase 4.**

---

## Phase 4: Rule Proposals

Для каждого выбранного правила выведи полное содержимое:

```
### N. rule-name-kebab-case

**Тип**: 🌍 Global | 📁 Project
**Pre-Check**: ✅ A1 ✅ A2 ✅ A3
**Путь**: `.claude/rules/<категория>/rule-name.md`

**Причина:**
- Почему важно?
- Какую проблему предотвращает?

**Содержимое файла:**

# Rule Title

[Описание принципа]

## Правильно

```language
// пример правильного кода
```

## Неправильно

```language
// пример неправильного кода
```

## Чеклист

- [ ] Пункт 1
- [ ] Пункт 2

---
```

---

## Phase 5: User Selection

После вывода предложений:

```
## Выбор

Ответь в формате: `1✅, 2❌, 3✏️ сделай короче`

- ✅ — создать как есть
- ❌ — отклонить
- ✏️ — модифицировать (укажи что изменить)

Или: `all` для всех, `0` для отмены.
```

---

## Phase 6: File Creation

**При создании:**

1. Проверь что файл не существует: `Glob(".claude/rules/**/<name>.md")`
2. Создай файл: `.claude/rules/<категория>/<name>.md`
3. Подтверди: `✅ Создано: .claude/rules/<категория>/<name>.md`

**Если категория новая:**
- Создай папку
- Сообщи: `📁 Создана новая категория: <name>/`

**Если файл существует:**
- Спроси: `⚠️ Файл существует. Перезаписать? (y/n/merge)`

---

## Критические правила

1. **НЕ создавай автоматически** — только после явного выбора пользователя
2. **НЕ предлагай тривиальные** — Quality Filter обязателен
3. **< 500 строк** — большие разбивай на несколько
4. **Self-contained** — понятно без контекста сессии
5. **Actionable** — конкретные инструкции, не советы
6. **БЕЗ frontmatter** — Claude Code rules это чистый Markdown

---

## Категоризация

| Тип | Когда использовать | Путь |
|-----|-------------------|------|
| 🌍 **Global** | Принцип для ЛЮБОГО проекта | `~/.claude/rules/` |
| 📁 **Project** | Специфика текущего проекта/стека | `.claude/rules/<категория>/` |

**Рекомендации по категориям:**
- Изучи существующие категории в проекте
- Предложи наиболее подходящую
- Или предложи новую с обоснованием

---

## Аргументы команды

- Без аргументов: полный workflow (память + сессия, если доступна)
- `$ARGUMENTS` = `mem`: только 🧠 From Memory (требует claude-mem, иначе ошибка)
- `$ARGUMENTS` = `nomem`: пропустить claude-mem даже если доступен
- `$ARGUMENTS` = `session`: только анализ сессии (пропустить claude-mem)
- `$ARGUMENTS` = `global`: только 🌍 Global правила
- `$ARGUMENTS` = `project`: только 📁 Project правила
- `$ARGUMENTS` = `verbose`: показать также REJECT кандидатов

---

## Примеры качественных правил

| Тип | Примеры |
|-----|---------|
| **Архитектура** | Модули, слои, naming conventions |
| **Workflow** | TDD, code review, git workflow |
| **Интеграции** | API retry, error handling, env config |
| **Debug** | Формат логов, трейсинг |
| **Domain** | Бизнес-логика, термины, связи |

---

## Начни

Phase -1 (claude-mem) → Phase 0 (структура) → Phase 1 (анализ) → Phase 2 (фильтр) → Phase 3 (индекс) → ответ → Phase 4-6
