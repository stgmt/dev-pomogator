// Zellij KDL layout — fresh `claude` (no resume) in given worktree
// Placeholders:
//   __CWD__   — absolute path to worktree
//   __NAME__  — pane title
layout {
    pane name="__NAME__" {
        cwd "__CWD__"
        command "claude"
    }
}
