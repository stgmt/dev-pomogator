# Design

## Реализуемые требования

- [FR-1: {Название}](FR.md#fr-1-{название})
- [FR-2: {Название}](FR.md#fr-2-{название})

## Компоненты

- `{Компонент 1}` — {описание}
- `{Компонент 2}` — {описание}

## Где лежит реализация

- App-код: `{путь/к/коду}`
- Wiring: `{путь/к/wiring}`

## Директории и файлы

- `{путь/к/файлу1}`
- `{путь/к/файлу2}`

## Алгоритм

1. {Шаг 1}
2. {Шаг 2}
3. {Шаг 3}

## API

### {Endpoint 1}

- Method: `{GET/POST/PUT/DELETE}`
- Path: `{путь}`
- Request: `{описание запроса}`
- Response: `{описание ответа}`

## BDD Test Infrastructure (если применимо)

> Заполнять если фича создаёт/изменяет тестовые данные, требует setup/teardown, или использует shared fixtures.
> Если не применимо — удалить секцию или написать `N/A`.

### Существующие hooks

| Hook файл | Тип | Тег/Scope | Что делает |
|-----------|-----|-----------|------------|
| `{путь}` | {Before/AfterScenario} | {тег} | {описание} |

### Новые hooks (если нужны)

| Hook файл | Тип | Тег/Scope | Что делает | По аналогии с |
|-----------|-----|-----------|------------|---------------|
| `{путь}` | {AfterScenario} | {тег} | {cleanup} | {существующий hook} |

### Cleanup Strategy

{Порядок удаления тестовых данных, каскадные зависимости, rollback при ошибках}

### Test Data & Fixtures

| Fixture/Data | Путь | Назначение | Lifecycle |
|-------------|------|------------|-----------|
| `{имя}` | `{путь}` | {описание} | {per-scenario/per-feature/shared} |

### Shared Context / State Management

| Ключ | Тип | Записывается в | Читается в | Назначение |
|------|-----|----------------|------------|------------|
| `{ключ}` | `{тип}` | `{step/hook}` | `{step/hook}` | {описание} |

