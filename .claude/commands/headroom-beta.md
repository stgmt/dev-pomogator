---
description: Install or inspect the optional dev-pomogator Headroom beta.
allowed-tools: Bash, Read
---

Run the requested Headroom beta operation from the plugin root.

Examples:

```bash
npx tsx tools/headroom-beta/install.ts --enable --topology codex-sub2api --runtime auto --dry-run
npx tsx tools/headroom-beta/install.ts --enable --topology codex-sub2api --runtime auto
```

Never paste or print API keys, OAuth tokens, refresh tokens, or generated
database secrets in the chat transcript.
