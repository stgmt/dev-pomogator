{
  "mcpServers": {
    "dev-pomogator-specs": {
      "type": "stdio",
      "command": "node",
      "args": [
        "${workspaceFolder}/tools/spec-mcp-server/server.bundle.mjs"
      ],
      "env": {
        "DEV_POMOGATOR_REPO_ROOT": "${workspaceFolder}"
      }
    }
  }
}
