---
name: docker-optimize
description: >
  Analyze Dockerfiles and docker-compose files for build optimization opportunities.
  Suggests layer caching, .dockerignore, BuildKit cache mounts, multi-stage splits.
  Use when Docker builds are slow or when setting up Docker for a new project.
allowed-tools: Read, Glob, Grep, Bash, Edit, Write, AskUserQuestion, Agent
---

> **Stub (retired mirror).** This skill is provided by the canonical Claude Code
> copy: [`.claude/skills/docker-optimize`](../.claude/skills/docker-optimize/SKILL.md).
> Use that copy — it is the single distributed source. This stub exists only so
> legacy paths under `.agents/skills/` keep resolving; nothing here is
> maintained and no payload (references/scripts/evals/templates) is duplicated.
