---
name: dedup-tests
description: >
  Scan test files for duplicated code using jscpd, classify findings, extract shared helpers.
  Use when dedup_stop hook blocks or manually to check for test code duplication.
user-invocable: true
allowed-tools: Read, Bash, Glob, Grep, Edit, Write, AskUserQuestion
---

> **Stub (retired mirror).** This skill is provided by the canonical Claude Code
> copy: [`.claude/skills/dedup-tests`](../.claude/skills/dedup-tests/SKILL.md).
> Use that copy — it is the single distributed source. This stub exists only so
> legacy paths under `.agents/skills/` keep resolving; nothing here is
> maintained and no payload (references/scripts/evals/templates) is duplicated.
